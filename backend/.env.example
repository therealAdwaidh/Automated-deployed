DEBUG=False
SECRET_KEY=your-secret-key-here-generate-a-strong-one
ALLOWED_HOSTS=localhost,127.0.0.1,your-backend-domain.com
CORS_ALLOWED_ORIGINS=http://localhost:3000,https://automated-deployed.vercel.app
# For local development (uses SQLite)
# DATABASE_URL is not needed - defaults to SQLite

# For Railway PostgreSQL (Railway auto-sets this):
# DATABASE_URL=postgresql://user:password@host:port/dbname
